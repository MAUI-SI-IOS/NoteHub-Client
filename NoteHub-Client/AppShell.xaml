<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NoteHub_Client.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NoteHub_Client"
    Title="NoteHub_Client"
    FlyoutBehavior="Flyout">

    <Shell.FlyoutHeader>
        <VerticalStackLayout Padding="20" BackgroundColor="#F0F0F0">
            <Label Text="NoteHub" FontSize="24" FontAttributes="Bold" TextColor="Black" />
        </VerticalStackLayout>
    </Shell.FlyoutHeader>

    <FlyoutItem Title="Main page" Icon="appicon.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>
    <FlyoutItem Title="Write Note" Icon="appicon.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:NoteDetailsPage}" />
    </FlyoutItem>
    
    <FlyoutItem Title="Search Note">
        <ShellContent ContentTemplate="{DataTemplate local:SearchPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Select server">
        <ShellContent ContentTemplate="{DataTemplate local:ServerSelectionPage}" />
    </FlyoutItem>

   
</Shell>
